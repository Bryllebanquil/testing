# Minimal Client Requirements
# Core dependencies only for basic client functionality

# Essential communication
python-socketio>=5.13.0
requests>=2.32.4

# Screen capture
mss>=10.0.0

# System monitoring
psutil>=5.9.5

# Security
cryptography>=41.0.3

# Audio (optional but recommended)
pyaudio>=0.2.11; sys_platform == "win32"
sounddevice>=0.5.2; sys_platform != "win32"

# Input control (optional but recommended)
pynput>=1.8.1

# Image processing (optional but recommended)
pillow>=9.5.0
numpy>=1.24.4